# Keep short_description aligned with the SKILL.md frontmatter description (source of truth).
interface:
  display_name: "Writing Style"
  short_description: "Direct Australian English without common AI tells"
  default_prompt: "Use $writing-style to write or rewrite this in direct Australian English without changing its meaning."

policy:
  allow_implicit_invocation: false
